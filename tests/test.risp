" Utilities for test "
" vim: set ft=lisp : "

(def assert (func (operator wanted actual)
  (if (not (operator wanted actual))
    (panic! (list "assertion unsatisfied" (list operator wanted actual))))))

(assert = 1 1)
